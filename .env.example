# ===== 复制为 .env 并填写 =====

# DashScope（阿里云）嵌入 + LLM
DASHSCOPE_API_KEY=your-dashscope-api-key
# 嵌入模型
EMBEDDING_MODEL=text-embedding-v2
# LLM 模型
LLM_MODEL=qwen-plus

# 向量库（ChromaDB）持久化目录
CHROMA_DIR=./chroma_data

# 结构化数据（比赛/赔率）SQLite 落地路径
DB_PATH=./data/football.db
